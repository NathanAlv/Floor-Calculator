import { Picker as ReactNativePicker } from '@react-native-picker/picker';
import { styled } from 'nativewind';
import { Text as ReactNativeText, TextInput as ReactNativeTextInput, TouchableOpacity as ReactNativeTouchableOpacity, View as ReactNativeView } from 'react-native';

export const View = styled(ReactNativeView);
export const Text = styled(ReactNativeText);
export const TextInput = styled(ReactNativeTextInput);
export const TouchableOpacity = styled(ReactNativeTouchableOpacity);
export const Picker = styled(ReactNativePicker);